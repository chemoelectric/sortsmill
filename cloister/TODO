* Do something like Cloister Lightface, using (for example)
  Style->ChangeGlyph with width/height of thin stems=95% and
  width/height of thick stems=90%.

  Alternatively, extrapolate a (not necessarily Lightface-like) light
  from the regular and the bold, and then touch it up.
